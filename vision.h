#ifndef VISION_H
#define VISION_H

#include "bitmap.h"

enum error_msg
{
    PATCH_SIZE_INCORRECT = -2,
    EDGE_LINES_NOT_VALID,
    ALL_OK = 0
};

typedef struct _edge_points
{
    // Sameer will upload this part of the code
} EdgePoints;

#endif